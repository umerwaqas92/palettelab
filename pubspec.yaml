name: palettelab
description: "Litur - Color Library App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_colorpicker: ^1.1.0
  camera: ^0.11.0+2
  image: ^4.2.0
  image_picker: ^1.1.2
  share_plus: ^12.0.1
  objective_c: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
